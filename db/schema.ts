/**
 * Drizzle schema entry point.
 *
 * No application tables are designed yet. `drizzle.config.ts` points here and
 * `pnpm db:generate` reads this file, so the D1 + Drizzle wiring stays in
 * place; add tables when the data model is decided, then generate a migration.
 */
export {};
