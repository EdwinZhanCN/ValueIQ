# Migrations

Generated D1 migrations land here once the application tables are designed.
`wrangler.jsonc` points `migrations_dir` at this directory, so the directory
stays in the repository while it is empty.
