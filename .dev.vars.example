# No secrets are needed for the scripted scaffold.
# When a model provider is selected, document its required secret here.
# Keep real values in .dev.vars locally and use wrangler secret put for deployment.
